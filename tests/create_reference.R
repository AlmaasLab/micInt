# # Temporary script designed to create unit tests
# write.table(x=ccrepe.results$p.values,file = 'tests/p.values.results.txt')
# write.table(x=ccrepe.results$q.values,file = 'tests/q.values.results.txt')
# write.table(x=ccrepe.results$sim.score,file = 'tests/sim.score.results.txt')
# write.table(x=ccrepe.results$z.stat,file = 'tests/z.stat.results.txt')
# write.table(x=ccrepe.results_absolute$p.values,file = 'tests/p.values.results_absolute.txt')
# write.table(x=ccrepe.results_absolute$q.values,file = 'tests/q.values.results_absolute.txt')
# write.table(x=ccrepe.results_absolute$sim.score,file = 'tests/sim.score.results_absolute.txt')
# write.table(x=ccrepe.results_absolute$z.stat,file = 'tests/z.stat.results_absolute.txt')
# 
